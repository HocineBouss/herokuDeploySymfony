{% extends 'layout.html.twig' %}

{% block title %}Articles{% endblock %}

{% block content %}
    <h1>Les Articles :</h1>
    
    <div class="row d-flex justify-content-center text-center">
        {% for article in articles %} {# articles est la variable qui represente mes articles recuperés dans ArtcielController (voir fonction allArticles() ) #}
            
            <div class="card m-1" style="width: 18rem;">
                <div class="card-body">

                    <a href="{{ path('modification_article', {id: article.id} ) }}">Editer </a>

                    <h5 class="card-title"> {{ article.titre }} </h5>
                    <h6 class="card-subtitle mb-2 text-muted">crée le: {{ article.dateDeCreation | date("d/m/Y \à H\\hi") }}</h6>
                    {% if article.dateUpdate is not null %}
                        <h6 class="card-subtitle mb-2 text-muted">modifié le: {{ article.dateUpdate | date("d/m/Y \à H\\hi") }}</h6>
                    {% endif %}
                    <h6>auteur : ...</h6>
                    {# si la date update de l'article n'est pas null, on affiche cette derniére #}
                    <p class="card-text">{{ article.contenu }}</p>
                    {# sur le lien lire l'article on cherche à aller sur la page unArticle et donc vers la route qui a pour name 'app_article', mais comme cette route a besoin d'un parametre qui' est l'id de l'article, on doit le lui préciser en 2éme parametre du path() #}
                   
                    <a href="{{ path('app_article' , {id: article.id} ) }}" class="btn btn-primary card-link">lire l'article</a>
                </div>
            </div>
                
        {% endfor %}
    </div>


{% endblock %}
